# This file isn't used by default.
# It's recommended to copy those settings into the ".git/config".

[core]
	whitespace = -blank-at-eol,space-before-tab,indent-with-non-tab,-tab-in-indent,blank-at-eof,tabwidth=8
